services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_USER: nest_user
      POSTGRES_PASSWORD: nest_dev_password
      POSTGRES_DB: nest_db
    ports:
      - '127.0.0.1:5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
